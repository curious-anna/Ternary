{
  "name": "recreate-excel",
  "version": "1.0.0",
  "description": "Excel-like input to pseudocode converter full-stack app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node testConversion.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
